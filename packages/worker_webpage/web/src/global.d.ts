/** The git commit this build was made from, baked in by the `define` option in `vite.config.ts`. */
declare const __COMMIT_SHA__: string;

/** The version number of `@webai/worker-webpage`, baked in by the `define` option in `vite.config.ts`. */
declare const __PACKAGE_VERSION__: string;
