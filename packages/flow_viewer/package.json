{
  "name": "@webai/flow-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "preflow_viewer": "npm run build:dependencies",
    "flow_viewer": "tsx src/cli.ts",
    "precli": "npm run build:dependencies",
    "cli": "tsx src/cli.ts",
    "predev": "npm run build:dependencies",
    "dev": "vite",
    "build": "vite build",
    "build:dependencies": "npm run build --workspace @webai/protocol",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json",
    "test": "npm run typecheck && npx tsx --test tests/index.test.ts"
  },
  "dependencies": {
    "@webai/protocol": "0.1.0",
    "commander": "^12.1.0",
    "express": "^4.21.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.2",
    "vite": "^6.4.3"
  }
}
