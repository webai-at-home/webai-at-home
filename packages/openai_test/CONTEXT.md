# Directory Context: `/packages/openai_test`

## Purpose

The one command line tool that tests, measures, and talks to a server speaking the OpenAI-compatible API. It answers three separate questions through three subcommands: `conformance` asks whether the server honours the protocol and what the model behind it can do, `benchmark` asks how fast it is, and `chat` shows what it actually answers.

## Key Exports & Entry Points

- `src/cli.ts`: the `openai_test` program and its subcommands.
- `src/`: every source folder — see [its own CONTEXT.md](src/CONTEXT.md).
- `tests/index.test.ts`: this package's own automated test suite, which starts its own local HTTP server and so needs neither a cluster nor a local model server.
- Command to run this folder: `npx tsx src/cli.ts chat --base_url http://localhost:1234/v1 --model <name> --prompt "..."`

## Rules

- This package imports nothing from this repository. Its only dependencies are `chalk`, `commander`, `openai`, and `@types/node`, so `npx tsx src/cli.ts` runs from a clean checkout with no workspace build first. Other packages may import this one; the arrow points one way only.
- Every request checks that the model identifier the endpoint named in its answer answers for the one that was requested, and fails the run when it does not. An endpoint answering as the wrong model is not always an error the endpoint reports.
- Never grade whether an answer is a good answer. A prompt built so that the behaviour under test is visible in the answer is a measurement; an opinion about whether the answer was helpful is not, and never appears.
- `PASS`, `FAIL`, `SKIP`, and `WARN` belong to `conformance` and to nothing else. `benchmark` reports numbers, and `chat` has no verdicts.
- Exit code `0` when nothing failed, `1` when a test failed, `2` when the run itself could not start. `chat` returns only `0` or `2`.
- A subcommand this program lists is a subcommand this program runs: `conformance` and `benchmark` are registered by the milestone that builds them, not before.

## Background

- This package merges [`packages/openai_api_tool`](../openai_api_tool/) and [`packages/openai_conformance_test`](../openai_conformance_test/), which measured the same server twice and shared two probers between them. Both stay, and both are frozen. See [issue #208](https://github.com/webai-at-home/webai-at-home/issues/208).
- The reported model identifier check comes from Milestone 0 of that issue, which found LM Studio 0.4.20 answering HTTP 200 to a request naming a model it cannot serve, generated by whichever model happened to be loaded.
