import { ExperimentPage } from './experiment_page.js';

///////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////
//	Main — the entry point of the Gemma 4 E2B WebGPU compute kernel experiment page
///////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////

export class Main {
	/**
	 * Builds the page and wires every button to its step.
	 *
	 * @returns Nothing.
	 */
	static start(): void {
		const experimentPage = new ExperimentPage();
		experimentPage.start();
	}
}

Main.start();
