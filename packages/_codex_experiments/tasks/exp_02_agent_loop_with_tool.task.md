Do these three steps, in order, in the current directory:

1. Create a file named `agent_loop.txt` whose only content is this one line: agent loop ready
2. Read `agent_loop.txt` back, and check that its content is exactly that line.
3. Run this command: ls agent_loop.txt

When all three steps are done, answer with exactly one word: done
