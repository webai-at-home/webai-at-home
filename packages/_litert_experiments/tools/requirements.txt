# Pinned so that a conversion that worked once works again. litert-torch pulls in PyTorch, which has no wheel for
# Python 3.14, so this environment needs Python 3.12. See README.md in this folder.
litert-torch==0.9.3
